version: 1
backend:
  phases:
    preBuild:
      commands:
        - npm install
    build:
      commands:
        - echo "Backend build complete"
        - node server.js &
  artifacts:
    baseDirectory: .
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/* 